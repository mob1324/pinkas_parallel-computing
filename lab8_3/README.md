# Лабораторная работа 8.3
Определение пропускной способности при передаче данных между CPU и GPU. 
Сравнение скорости работы стандартной страничной (Pageable) и закрепленной (Pinned) памяти.

## Сборка и запуск
```bash
nvcc src/bandwidth.cu -o bandwidth_app
./bandwidth_app
